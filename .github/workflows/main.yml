name: 🚀 Deploy website on push
on:
  push:

jobs:
  web-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code (front only, no submodules)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
          submodules: false
          sparse-checkout: |
            QuaiAntiqueRestaurantFront
          sparse-checkout-cone: true

      # Debug (facultatif) : voir ce qui a été récupéré
      - name: Tree
        run: |
          pwd
          ls -la
          find . -maxdepth 3 -type f -name "package.json" -o -name ".gitmodules" -print

      # Node seulement si un package.json existe dans le front
      - name: Setup Node
        if: ${{ hashFiles('QuaiAntiqueRestaurantFront/package.json') != '' }}
        uses: actions/setup-node@v5
        with:
          node-version: 20

      - name: Install npm dependencies
        if: ${{ hashFiles('QuaiAntiqueRestaurantFront/package.json') != '' }}
        run: |
          c
